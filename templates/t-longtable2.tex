\documentclass{article}
\usepackage{geometry}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{tabularx}
\geometry{
a4paper,
left=15mm,
right=15mm,
top=15mm,
bottom=15mm,
}
\usepackage{graphicx}
\usepackage{amsfonts}
\parindent0mm



{% macro april(sid) %}
        \noindent\parbox[c][5mm][c]{5mm}{
            \centering
            \includegraphics[width=4mm]{{ '{'}}{{  short2object[sid].fn }}}
            }
        
{% endmacro %}
\begin{document}

{% for sheet in all_sheets %}

    \begin{tabular}{l l l}
    
    \large {{ sheet.name}} &  \large Unterschrift &
        \multirow{2}{*}{
            \includegraphics[width=4cm]{{ '{'}}{{ sheet.qr }}}
        } 
    \\
    \\
    
    \hspace{ .3\textwidth } & \hspace{ .3\textwidth}  \\
    
    \\
    
    \multicolumn{ 2 }{ p{.7\textwidth} }{
    Hinweise:
    
    alle Fragen koennen mehrere oder auch keine richtige Antwort haben.
    
    Zum korrigieren verwenden Sie die naechste Spalte der gleichen Frage.
    
    Ist eine Spalte ausgefuellt, sind alle davorliegenden Spalten der gleichen Frage ungueltig.
    
    Die Barcodes und Marker duerfen nicht veraendert werden.
    
    
    
    } \\
    \end{tabular}
    
    
    
    {% for i in sheet.questions %}
        
    
        \section*{ {{ i.q }}\hfill({{i.points}}P)} 
    
        \begin{minipage}{.8\textwidth}
            \begin{tabularx}{\textwidth}{r | c c c | c}
                {% for jj in range(3) %}
                  & {{ april("col." + jj | string ) }}
                {% endfor %}
                \\

                {% for jj in range(3) %}
                        & $\square$
                {% endfor %}
                \\
                \cline{2-4}
                
                {% for j in i.a %}
                    {{ j }} 
                    {% for i in range(3) %}
                        & $\square$
                    {%- endfor %}
                    & {{ april("row." + loop.index | string ) }}
                    \\
                {% endfor %}
                \cline{2-4}
            \end{tabularx}
        \end{minipage}
        \begin{minipage}{.1\textwidth}
            \includegraphics[width=25mm]{{ '{'}}{{ i.qr }}}
        \end{minipage}
    
    
    
    {% endfor %}
    
    \cleardoublepage
    
{% endfor %}

\end{document}
