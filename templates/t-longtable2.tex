\documentclass{article}
\usepackage{geometry}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{tabularx}
\geometry{
a4paper,
left=15mm,
right=15mm,
top=15mm,
bottom=15mm,
}
\usepackage{graphicx}
\usepackage{amsfonts}
\parindent0mm

\begin{document}

{% macro april(sid) %}
        \noindent\parbox[c][4mm][c]{4mm}{
            \centering
            \includegraphics[width=3mm]{{ '{'}}{{  short2object[sid].fn }}}
            }
        
{% endmacro %}
{% set ma = score['basics']['max_answers'] %}
{% set mac = ma + 2 %}

\begin{tabular}{l l l}

\large Name &  \large Unterschrift &
    \multirow{2}{*}{
        \includegraphics[width=4cm]{{ '{'}}{{ score.basics.qr }}}
    } 
\\
\\

\hspace{ .3\textwidth } & \hspace{ .3\textwidth}  \\

\\

\multicolumn{ 2 }{ p{.7\textwidth} }{
Hinweise:

alle Fragen koennen mehrere oder auch keine richtige Antwort haben.

Zum korrigieren verwenden Sie die naechste Zeile der gleichen Frage.

Ist eine weiter Zeile ausgefuellt, sind alle darueberliegenden Zeilen der gleichen Frage ungueltig.
} \\
\end{tabular}



{% for i in qlatex %}
    

    \section*{ {{ i.q }}\hfill({{i.points}}P)} 

    \begin{minipage}{.8\textwidth}
        \begin{tabularx}{\textwidth}{l | c c c | c}
            {% for jj in range(3) %}
              & {{ april("col." + jj | string ) }}
            {% endfor %}
            \\
            
            {% for j in i.a %}
                {{ j }} 
                {% for i in range(3) %}
                    & $\square$
                {%- endfor %}
                & {{ april("row." + loop.index | string ) }}
                \\
            {% endfor %}
        \end{tabularx}
    \end{minipage}
    \begin{minipage}{.1\textwidth}
        \includegraphics[width=2cm]{{ '{'}}{{ i.qr }}}
    \end{minipage}



{% endfor %}

Ende des Bogens.

\cleardoublepage

\end{document}
