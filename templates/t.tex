\documentclass{article}
\usepackage{a4wide}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{amsfonts}
\usepackage{tabularx}
\parindent0mm

\begin{document}

Name:
Unterschrift:

Hinweise:

alle Fragen koennen mehrere richtige Antworten haben.
alle Fragen koennen auch keine richtige Antwort beinhalten.

zum korrigieren verwenden Sie die naechste Zeile des Antwortbogens der gleichen Frage.
Ist eine weiter Zeile ausgefuellt, sind alle darueberliegenden Zeilen nicht gueltig.

{% for i in qlatex %}
    {{ loop.index }}: {{ i.question }}

    \begin{enumerate}
    {% for j in i.answers %}
        \item {{ j }}
    {%- endfor %}
    \end{enumerate}
{%- endfor %}

{% macro col2() %}
    {% for row in range(score['basics']['max_answers'] ) %}
        & {{ loop.index }}
    {% endfor %}
{% endmacro %}

{% macro cols(begend="X", bottom=False ) %}
    {% if bottom %}
    &
        {% for row in range(score['basics']['max_answers'] ) %}
            & {{ loop.index }}
        {% endfor %}
        & \\
        & & 
    {% else %}
        &  &
    {% endif %}
    
    {% for row in range(score['basics']['max_answers'] ) %}
        \noindent\parbox[c]{7mm}{
            \includegraphics[width=5mm]{{ '{' }}{{cfg.static_qr_dir}}/r.{{begend}}.{{ row }}.png}
            } &
    {% endfor %}
    \\
    {% if bottom %}
    {% endif %}
    
    {% if not bottom %}
        &
        {% for row in range(score['basics']['max_answers'] ) %}
            & {{ loop.index }}
        {% endfor %}
        &
        \\
        \hline
        \hline
    {% endif %}
{%- endmacro %}

\newpage
Antwortbogen:

\includegraphics[width=6cm]{{ '{'}}{{ score.basics.qr }}}
\begin{center}


\begin{tabular}{ | c | c | {{ '| c' * score['basics']['max_answers'] }} || c |}
\hline
{{ cols(begend='b', bottom=False) }} 


{% for line in range(score['card'] | length) %}
    {% for rep in range(cfg.num_repeats | int) %}
    \noindent\parbox[c]{7mm}{
        \includegraphics[width=5mm]{{ '{' }}{{cfg.static_qr_dir}}/l.b.{{ line }}.{{rep}}.png}
        }

        & {{ line+1 }}.{{ rep+1 }} 
        {% for row in range(score['basics']['max_answers']) %}

            &
            {% if score['card'][line] is defined and score['card'][line]['answers'][row] is defined%}
                $\square$
            {% else %}

            {% endif %}
        {% endfor %}
        &
    \noindent\parbox[c]{7mm}{
         \includegraphics[width=5mm]{{ '{' }}{{cfg.static_qr_dir}}/l.e.{{ line }}.{{rep}}.png}
         }
        \\
    {% endfor %}
    \hline 
{% endfor %}

\hline \\
{{ cols(begend='e', bottom=True) }}
\hline

\end{tabular}
\end{center}

\newpage


\end{document}
