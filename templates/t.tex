\documentclass{article}
\usepackage{geometry}
\geometry{
a4paper,
}
\usepackage{graphicx}
\usepackage{amsfonts}
\usepackage{tabularx}
\usepackage[inline,shortlabels]{enumitem}
\parindent0mm

\begin{document}

Name:
Unterschrift:

Hinweise:

alle Fragen koennen mehrere richtige Antworten haben.
alle Fragen koennen auch keine richtige Antwort beinhalten.

zum korrigieren verwenden Sie die naechste Zeile des Antwortbogens der gleichen Frage.

Ist eine weiter Zeile ausgefuellt, sind alle darueberliegenden Zeilen mit der gleichen Fragennummer ungueltig.

{% for i in qlatex %}
    {{ loop.index }}: {{ i.question }}

    \begin{center}
    \begin{enumerate*}[series=tobecont, itemjoin=\quad]
    {% for j in i.answers %}
        \item {{ j }}
    {%- endfor %}
    \end{enumerate*}
    \end{center}

{%- endfor %}


\newpage
Antwortbogen:

\includegraphics[width=6cm]{{ '{'}}{{ score.basics.qr }}}
\begin{center}


\begin{tabular}{ c | c | {{ '| c' * score['basics']['max_answers'] }} || c }

\multicolumn{2}{c||}{ } 
{% for row in range(score['basics']['max_answers']) %}
        & \includegraphics{{ '{'}}{{  short2object["{}.{}.{}".format('row', 'begin', row)].filename }}}
{% endfor %}
\\
\multicolumn{2}{c||}{ } 
{% for row in range(score['basics']['max_answers']) %}
        & {{ row + 1 }}
{% endfor %}
\\

\hline
\hline

{% for line in range(score['card'] | length) %}
    {% for rep in range(cfg.num_repeats | int) %}
        \includegraphics{{ '{'}}{{  short2object["{}.{}.{}.{}".format('line', 'begin', line, rep)].filename }}}
        & {{ line+1 }}.{{ rep+1 }} 
         
        {% for row in range(score['basics']['max_answers']) %}
            &
            {% if score['card'][line] is defined and score['card'][line]['answers'][row] is defined%}
                $\square$
            {% else %}

            {% endif %}
        {% endfor %}
        &
        \includegraphics{{ '{'}}{{  short2object["{}.{}.{}.{}".format('line', 'end', line, rep)].filename }}}
        \\
    {% endfor %}
    \hline 
{% endfor %}


\multicolumn{2}{c||}{ } 
{% for row in range(score['basics']['max_answers']) %}
        & {{ row + 1 }}
{% endfor %}
\\
\multicolumn{2}{c||}{ } 
{% for row in range(score['basics']['max_answers']) %}
        & \includegraphics{{ '{'}}{{  short2object["{}.{}.{}".format('row', 'begin', row)].filename }}}
{% endfor %}

\end{tabular}
\end{center}

\newpage


\end{document}
