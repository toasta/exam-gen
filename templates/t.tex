\documentclass{article}
\usepackage{a4wide}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{amsfonts}
\usepackage{tabularx}
\parindent0mm

\begin{document}
\newlist{checkboxes}{itemize}{2}
\setlist[checkboxes]{label=$\square$}
{% for i in qlatex %}
    {{ loop.index }}: {{ i.question }}

    \begin{checkboxes}
    {% for j in i.answers %}
        \item {{ j }}
    {%- endfor %}
    \end{checkboxes}
{%- endfor %}

\newpage
\begin{center}
\includegraphics[width=5cm]{{ '{'}}{{ score.basics.qr }}}


\begin{tabular}{ | c | c | {{ '| c' * score['basics']['max_answers'] }} | c |}
\hline
&
{% for row in range(score['basics']['max_answers'] ) %}
    & \includegraphics{{ '{' }}{{cfg.static_qr_dir}}/r.b.{{ row }}.png}
{% endfor %}
&
\\
\hline

{% for line in range(score['card'] | length) %}
    {% for rep in range(cfg.num_repeats | int) %}
        \includegraphics{{ '{' }}{{cfg.static_qr_dir}}/l.b.{{ line }}.{{rep}}.png}

        & {{ line }}.{{ rep }} 
        {% for row in range(score['basics']['max_answers']) %}

            &
            {% if score['card'][line] is defined and score['card'][line]['answers'][row] is defined%}
                $\square$
            {% else %}

            {% endif %}
        {% endfor %}
        & \includegraphics{{ '{' }}{{cfg.static_qr_dir}}/l.e.{{ line }}.{{rep}}.png}
        \\
    {% endfor %}
    \hline
{% endfor %}

&
{% for row in range(score['basics']['max_answers'] ) %}
    & \includegraphics{{ '{' }}{{cfg.static_qr_dir}}/r.e.{{ row }}.png}
{% endfor %}
&
\\
    \hline
\end{tabular}
\end{center}

\newpage


\end{document}
