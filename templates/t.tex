\documentclass{article}
\usepackage{a4wide}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{amsfonts}
\usepackage{tabularx}
\parindent0mm

\begin{document}

Name:
Unterschrift:

Hinweise:

alle Fragen koennen mehrere richtige Antworten haben.
alle Fragen koennen auch keine richtige Antwort beinhalten.

zum korrigieren verwenden Sie die naechste Zeile des Antwortbogens der gleichen Frage.
Ist eine weiter Zeile ausgefuellt, sind alle darueberliegenden Zeilen nicht gueltig.

{% for i in qlatex %}
    {{ loop.index }}: {{ i.question }}

    \begin{enumerate}
    {% for j in i.answers %}
        \item {{ j }}
    {%- endfor %}
    \end{enumerate}
{%- endfor %}

\newpage
Antwortbogen:

\includegraphics[width=4cm]{{ '{'}}{{ score.basics.qr }}}
\begin{center}


\begin{tabular}{ | c | c | {{ '| c' * score['basics']['max_answers'] }} | c |}
\hline
&
{% for row in range(score['basics']['max_answers'] ) %}
    & \includegraphics[width=10mm]{{ '{' }}{{cfg.static_qr_dir}}/r.b.{{ row }}.png}
{% endfor %}
&
\\
\hline

{% for line in range(score['card'] | length) %}
    {% for rep in range(cfg.num_repeats | int) %}
    \noindent\parbox[c]{10mm}{
        \includegraphics[width=10mm]{{ '{' }}{{cfg.static_qr_dir}}/l.b.{{ line }}.{{rep}}.png}
        }

        & {{ line+1 }}.{{ rep+1 }} 
        {% for row in range(score['basics']['max_answers']) %}

            &
            {% if score['card'][line] is defined and score['card'][line]['answers'][row] is defined%}
                $\square$
            {% else %}

            {% endif %}
        {% endfor %}
        &
    \noindent\parbox[c]{10mm}{
         \includegraphics[width=10mm]{{ '{' }}{{cfg.static_qr_dir}}/l.e.{{ line }}.{{rep}}.png}
         }
        \\
    {% endfor %}
    \hline
{% endfor %}

&
{% for row in range(score['basics']['max_answers'] ) %}
    & 
    \noindent\parbox[c]{10mm}{
    \includegraphics[width=10mm]{{ '{' }}{{cfg.static_qr_dir}}/r.e.{{ row }}.png}
    }
{% endfor %}
&
\\
    \hline
\end{tabular}
\end{center}

\newpage


\end{document}
